import * as RDF from 'rdf-js';
export interface IQuadTerms<Q extends RDF.BaseQuad = RDF.Quad> {
    subject?: Q['subject'];
    predicate?: Q['predicate'];
    object?: Q['object'];
    graph?: Q['graph'];
}
declare const _default: {
    toBeRdfDatasetMatching<Q extends RDF.BaseQuad = RDF.Quad>(dataset: RDF.DatasetCore<Q, Q>, { subject, predicate, object, graph }: IQuadTerms<Q>, matches?: number): {
        message: () => string;
        pass: boolean;
    };
};
export default _default;
