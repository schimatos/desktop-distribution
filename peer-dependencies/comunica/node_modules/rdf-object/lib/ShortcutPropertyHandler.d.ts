import { JsonLdContextNormalized } from "jsonld-context-parser";
/**
 * A proxy handler for exposing a URI-to-? map to shortcut-to-? map
 * based on a JSON-LD context.
 */
export declare class ShortcutPropertyHandler<T> implements ProxyHandler<{
    [predicate: string]: T[];
}> {
    private readonly context;
    constructor(context: JsonLdContextNormalized);
    has(target: {
        [predicate: string]: T[];
    }, p: PropertyKey): boolean;
    get(target: {
        [predicate: string]: T[];
    }, p: PropertyKey): T[];
    set(target: {
        [predicate: string]: T[];
    }, p: PropertyKey, value: any): boolean;
    ownKeys(target: {
        [predicate: string]: T[];
    }): PropertyKey[];
    private toTermString;
}
