import { Resource } from "rdf-object";
import { IFetchOptions, ITestCase } from "rdf-test-suite";
import { LdfResponseMockerFactory } from "../../factory/LdfResponseMockerFactory";
import { ILdfQueryEngine } from "./ILdfQueryEngine";
import { ILdfTestCaseHandler } from "./ILdfTestCaseHandler";
/**
 * A Linked Data Fragments test case data holder.
 */
export interface ILdfTestCase extends ITestCase<ILdfQueryEngine> {
    type: 'ldf';
}
/**
 * Create a test case object from a resource.
 * @param {{[uri: string]: ITestCaseHandler<ITestCase<any>>}} testCaseHandlers Handlers for constructing test cases.
 * @param {string} cachePath The base directory to cache files in. If falsy, then no cache will be used.
 * @param {Resource} resource A resource.
 * @return {Promise<ITestCase<any>>} A promise resolving to a test case object.
 */
export declare function ldfTestCaseFromResource(factory: LdfResponseMockerFactory, testCaseHandlers: {
    [uri: string]: ILdfTestCaseHandler<ITestCase<any>>;
}, options: IFetchOptions, resource: Resource): Promise<ITestCase<any>>;
